import { ExpenseListItem } from "../types";

// PLACEHOLDER: no real API endpoint yet for listing expenses.
// Swap this out once the TL provides the actual endpoint.
export const fetchExpenses = async (): Promise<ExpenseListItem[]> => {
  return [];
};